import * as React from 'react';

export interface ITodosCardProps {
}

export default function TodosCard (props: ITodosCardProps) {
  return (
    <div className='w-full flex flex-col h-auto'>
      
    </div>
  );
}
